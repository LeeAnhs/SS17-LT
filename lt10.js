let n = prompt("Vui lòng nhập vào một số bất kỳ:");
n = parseFloat(n);
let sqrt = Math.sqrt(n);
let result = (sqrt === Math.floor(sqrt));
if (result = (sqrt === Math.floor(sqrt))) {
    document.write(result = n + "    " + "là số chính phương") ;
} else {
    document.write(result = n + "    " +"không phải là số chính phương");
}

